{
  "name": "durak-online",
  "version": "1.0.0",
  "description": "Çevrim içi Durak kart oyunu",
  "private": true,
  "scripts": {
    "dev": "pnpm -C apps/server dev & pnpm -C apps/web dev",
    "build": "pnpm -C apps/server build && pnpm -C apps/web build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}